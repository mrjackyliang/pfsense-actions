/**
 * Text mac address lower.
 *
 * @since 1.0.0
 */
export const textMacAddressLower = /^([0-9a-f]{2}:){5}([0-9a-f]{2})$/;

/**
 * Text mac address upper.
 *
 * @since 1.0.0
 */
export const textMacAddressUpper = /^([0-9A-F]{2}:){5}([0-9A-F]{2})$/;
